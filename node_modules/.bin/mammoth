#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mammoth/bin/mammoth" "$@"
  ret=$?
else 
  node  "$basedir/../mammoth/bin/mammoth" "$@"
  ret=$?
fi
exit $ret
